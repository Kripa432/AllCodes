#include<stdio.h>
#include<string.h>
#include<ctype.h>
#include<math.h>
#include<stdbool.h>

#define max(x,y) ((x) > (y) ? (x) : (y))
#define min(x,y) ((x) < (y) ? (x) : (y))
#define abs(x) ((x) >= 0 ? (x) : -(x))
#define sgn(x) ((x) > 0 ? 1 : ((x) < 0 ? -1 : 0))

int main()
{
    long long int i,j,c,len,num,temp,t,res,x,y,sum;
    
    scanf("%lld",&t);
    while(t--)
    {
    	scanf("%lld %lld %lld",&x,&y,&sum);
    	d=(y-x)/(n-5)
    }

    return 0;
}
